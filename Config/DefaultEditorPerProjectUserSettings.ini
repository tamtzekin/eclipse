[ContentBrowser]
ContentBrowserTab1.SelectedPaths=/Game/ThirdPersonCPP

; Stop PIE from intercepting the Esc key — let it pass through to the player
; controller so the in-game pause menu actually opens. Without this, Esc
; inside PIE just exits Play mode.
[/Script/UnrealEd.LevelEditorPlaySettings]
bStopOnEscape=False
